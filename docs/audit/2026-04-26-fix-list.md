# Bantu Platform Audit 2026-04-26 — Fix List

> Input for writing-plans. Sort: Critical first, then High; within severity by domain (Security → Business Logic → Code Quality → Performance → Schema).

| ID | Title | File | Severity | Domain |
|---|---|---|---|---|
| S-001 | `GET /api/employees/:id` returns full employee record including TIN, passport, SSN | `backend/routes/employees.js:350-371` | High | Security |
| S-002 | `POST /api/backup/restore` still allows cross-tenant `companyId` reassignment via raw item upsert | `backend/routes/backup.js:118-126` | High | Security |
| S-003 | `/uploads/documents/*` is served by `express.static` with no authentication | `backend/index.js:61` | High | Security |
| S-004 | `/api/period-end` POST cross-tenant data corruption — `loanRepayment.updateMany` not scoped to client | `backend/routes/periodEnd.js:49-58` | High | Security |
| S-005 | `payrollCalendar.js` `GET /:id` and `POST /:id/close` lack ownership checks | `backend/routes/payrollCalendar.js:73-85` | High | Security |
| S-025 | JWT stored in `sessionStorage` is readable by any JS — XSS = full account takeover | `frontend/src/lib/auth.ts:21-23` | High | Security |
| S-026 | Public route guard relies on client-side `parseJwt` of an unvalidated token — bypassable via crafted JWT | `frontend/src/lib/auth.ts:12-19` | High | Security |
| S-027 | No protected catch-all — unauthenticated users sent to `/login` only after the role lookup, leaking deep-link page-shell network calls | `frontend/src/App.tsx:264-269` | High | Security |
| S-028 | `/api/backup/restore` exposed to all CLIENT_ADMINs in the UI — pairs with backend mass-assignment vector (S-002) | `frontend/src/pages/utilities/BackupRestore.tsx:53-71` | High | Security |
| B-001 | Year-end PAYE reconciliation uses calendar year instead of Zimbabwe tax year | `backend/routes/payroll/reports.js:34-36` | High | Business Logic |
| B-002 | `taxEngine.js` `DEFAULT_NSSA_CEILING` silently used by `services/backPayService.js` and any caller that omits `nssaCeiling` | `backend/utils/taxEngine.js:21` | High | Business Logic |
| B-003 | Split-salary apportionment uses cash-only ratio but applies it to PAYE that includes benefits | `backend/utils/taxEngine.js:317-369` | High | Business Logic |
| B-004 | Split-salary engine forces consolidated NSSA ceiling to USD; ZiG ceiling parameter is ignored | `backend/utils/taxEngine.js:308-312` | High | Business Logic |
| B-005 | FDS_AVERAGE forecasting averages by `(uniqueMonths + 1)` but cumGross is a sum of all prior payslips | `backend/routes/payroll/process.js:543-565` | High | Business Logic |
| B-006 | Loan deemed-interest benefit pulls only PAID repayments, not fully repaid principal | `backend/routes/payroll/process.js:473-481` | High | Business Logic |
| B-041 | Exchange rate validated as `> 1` but ZiG/USD rate could legitimately be below 2 — threshold is business-logic assumption baked into UI | `frontend/src/pages/Payroll.tsx:104` | High | Business Logic |
| B-042 | `totalDeductions` in `Payslips.tsx` computed as `gross − netPay` on the frontend, diverging from backend's summed statutory fields | `frontend/src/pages/Payslips.tsx:303` | High | Business Logic |
| B-043 | No date-range validation: `endDate` can be before `startDate` in `PayrollNew.tsx` | `frontend/src/pages/PayrollNew.tsx:96-117` | High | Business Logic |
| B-044 | Payroll "Process" action available on `DRAFT` runs bypassing the submit→approve workflow | `frontend/src/pages/Payroll.tsx:263-271` | High | Business Logic |
| B-054 | `LeaveNew.tsx` days calculation counts calendar days — weekends and public holidays inflate the leave deduction | `frontend/src/pages/LeaveNew.tsx:23-25` | High | Business Logic |
| B-055 | `LeaveNew.tsx` / `LeaveEdit.tsx` — no balance check before submission; leave can be recorded beyond available balance | `frontend/src/pages/LeaveNew.tsx:27-39` | High | Business Logic |
| B-056 | `LeaveEncashments.tsx` estimated amount preview is always `USD 0.00` — broken formula produces no useful information | `frontend/src/pages/LeaveEncashments.tsx:232-235` | High | Business Logic |
| B-057 | `BackPay.tsx` effective date accepts future dates — back pay can be committed for periods that have not yet occurred | `frontend/src/pages/utilities/BackPay.tsx:172-177` | High | Business Logic |
| C-001 | `services/attendanceService.js` — no top-level try/catch; all Prisma calls unguarded | `backend/services/attendanceService.js:16` | High | Code Quality |
| C-002 | `lib/systemSettings.js` — `_loadAll` async with no try/catch; Prisma error crashes every settings-dependent payroll route | `backend/lib/systemSettings.js:9` | High | Code Quality |
| C-003 | `lib/companyContext.js` — `getCompanyForUser`/`getClientIdForUser` async with no try/catch; middleware-level unhandled rejection | `backend/lib/companyContext.js:9` | High | Code Quality |
| C-004 | `utils/intelligenceEngine.js` — exported functions have no try/catch; Prisma errors crash the route handler | `backend/utils/intelligenceEngine.js:3` | High | Code Quality |
| C-005 | `utils/intelligenceEngine.js` `predictCashflow` references `input.amount` which does not exist on `PayrollInput` — `stagedInputsTotal` is always `0` | `backend/utils/intelligenceEngine.js:204` | High | Code Quality |
| C-017 | `AppShell.tsx` — `useEffect` for auto-logout has missing `handleLogout` dependency; stale closure captures initial `navigate` | `frontend/src/components/AppShell.tsx:81` | High | Code Quality |
| C-018 | `AppShell.tsx` — `loadCompanies` captured in `useEffect` with empty dep array; `user` role check uses stale closure | `frontend/src/components/AppShell.tsx:61` | High | Code Quality |
| C-019 | `useIdleTimer.ts` — activity handler captures `isWarning` from closure; stale value causes reset to fire during warning window | `frontend/src/hooks/useIdleTimer.ts:55` | High | Code Quality |
| P-001 | N+1: `jobs/leaveAccrual.js` issues individual DB calls per employee×policy in the monthly cron loop | `backend/jobs/leaveAccrual.js:112-178` | High | Performance |
| P-002 | `GET /api/reports/tax` (P16): `include: { employee: true, payrollRun: { include: { company: true } } }` fetches all columns for every payslip row | `backend/routes/reports/statutory.js:50-58` | High | Performance |
| SC-001 | `PayrollRun` has no cascade on Company delete — orphaned run rows block tenant offboarding | `backend/prisma/schema.prisma` (PayrollRun model) | High | Schema |
| SC-002 | `Employee` → `Company` and `Employee` → `Client` relations have no `onDelete` — cascading deletes fail silently or produce FK violations | `backend/prisma/schema.prisma` (Employee model) | High | Schema |
| SC-003 | `Loan` → `Employee` has no cascade — deleting an employee leaves orphaned loans and repayments | `backend/prisma/schema.prisma` (Loan model) | High | Schema |
| SC-004 | `LeaveRecord` → `Employee` has no cascade — deleting an employee orphans leave history | `backend/prisma/schema.prisma` (LeaveRecord model) | High | Schema |
| SC-005 | `LeaveRequest` → `Employee` has no cascade — same orphan risk as LeaveRecord | `backend/prisma/schema.prisma` (LeaveRequest model) | High | Schema |
| SC-006 | `PayrollTransaction` → `Employee` has no cascade — payroll history orphaned on employee delete | `backend/prisma/schema.prisma` (PayrollTransaction model) | High | Schema |
| SC-007 | `CurrencyRate` has no unique constraint on `(companyId, fromCurrency, toCurrency, effectiveDate)` — duplicate rates for same day accepted silently | `backend/prisma/schema.prisma` (CurrencyRate model) | High | Schema |
| SC-008 | `SystemSetting` has no `companyId` — global settings cannot be overridden per-tenant; all tenants share a single row | `backend/prisma/schema.prisma` (SystemSetting model) | High | Schema |
